{% extends "layout.html.twig" %}

{% block header %}
    {% include "partials/_header-admin.html.twig" %}
{% endblock %}

{% block main %}
    <main class="container py-5">
        <h1>Détails remboursement</h1>
        <div class="card mt-3" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{ data.username }}</h5>
                <h6 class="card-subtitle mb-2 text-body-secondary">{{ data.email }}</h6>
                <p class="card-text">{{ data.role }}</p>
                <a href="index.php?route=updateRemboursement&id={{ data.id }}" class="card-link">Modifier</a>
                <a href="index.php?route=deleteRemboursement&id={{ data.id }}" class="card-link">Supprimer</a>
            </div>
        </div>
    </main>
{% endblock %}